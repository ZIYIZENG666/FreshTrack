<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:supportsRtl="true">

		<!-- 显式注册广播接收器 (可选，因为你用了 [BroadcastReceiver] 特性) -->
		<receiver android:name="crc64ec8a44d8d9e7f8a9.ReminderBroadcastReceiver"
                  android:enabled="true"
                  android:exported="false">
			<intent-filter>
				<action android:name="com.freshtrack.REMINDER_ACTION" />
			</intent-filter>
		</receiver>
	</application>

	<!-- ⚠️ 必需：Android 13+ 通知权限 -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

	<!-- ⚠️ 必需：闹钟唤醒权限 -->
	<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>